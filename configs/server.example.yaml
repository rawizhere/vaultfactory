server:
  host: "0.0.0.0"
  port: 8080

database:
  dsn: "postgres://login:password@localhost:5432/vaultfactory?sslmode=disable"

security:
  jwt_secret: "your-secret-key"
  encryption_key: "your-secret-key"
  jwt_expire_hours: 24
  refresh_token_expire_days: 30

logging:
  level: "info"
  format: "json"
  output: "stdout"
